---
phase: 01-complete-ch1
plan: 01-02
type: feature
---

<objective>
Create scene 7 (sc7) for Chapter 1 with wipe effect on background and pixel-appear animation loop.

Scene prompt (from scene.txt):
1. The bg.png opens with wipe effect from the middle to top and bottom
2. The frames 1.png and 2.png are in loop and positioned in the middle
3. The 1 and 2.png loop animation appears with pixel-appear effect after the bg appears
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Reference existing scenes:**
@ch1/sc6/scene.json
@ch1/sc6/sc6-intro.html
@ch1/sc1/scene.json

**Assets available:**
- ch1/sc7/bg.png (background image)
- ch1/sc7/1.png (animation frame 1)
- ch1/sc7/2.png (animation frame 2)
</context>

<tasks>

<task type="auto">
  <name>Create sc7 scene.json with effects configuration</name>
  <files>ch1/sc7/scene.json</files>
  <action>
Create scene.json with:
- duration: 12000ms
- background: bg.png with wipeFromMiddle effect
- autoAdvance: true, advanceDelay: 1000

Timeline:
1. time 0: Background appears with wipe-from-middle effect (CSS animation)
2. time 2500: After wipe completes, show looping sprite animation with pixel-appear effect
   - frames: ["1.png", "2.png"] in loop
   - position: centered in the middle of screen
   - effect: pixelAppear entry animation

Use custom effect config:
```json
{
  "backgroundEffect": {
    "type": "wipeFromMiddle",
    "duration": 2000
  },
  "timeline": [
    {
      "time": 2500,
      "action": "showSprite",
      "config": {
        "id": "characterLoop",
        "frames": ["1.png", "2.png"],
        "loop": true,
        "frameDelay": 500,
        "position": {"x": "center", "y": "center"},
        "entryEffect": "pixelAppear",
        "entryDuration": 1500
      }
    }
  ]
}
```
  </action>
  <verify>scene.json exists and is valid JSON</verify>
  <done>scene.json created with wipe and pixel-appear effect configuration</done>
</task>

<task type="auto">
  <name>Create sc7-intro.html standalone player with effects</name>
  <files>ch1/sc7/sc7-intro.html</files>
  <action>
Create standalone HTML player following sc6-intro.html pattern with:

1. Same base structure:
   - Links to design-system CSS and animation CSS
   - .screen-container wrapper (1280x800)
   - Centered body styling

2. Add CSS for wipe-from-middle effect:
```css
@keyframes wipeFromMiddle {
  0% {
    clip-path: polygon(0 50%, 100% 50%, 100% 50%, 0 50%);
  }
  100% {
    clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%);
  }
}
.scene-bg.wipe-effect {
  animation: wipeFromMiddle 2s ease-out forwards;
}
```

3. Add CSS for pixel-appear effect:
```css
@keyframes pixelAppear {
  0% {
    opacity: 0;
    filter: blur(10px) contrast(200%);
  }
  50% {
    opacity: 0.7;
    filter: blur(5px) contrast(150%);
  }
  100% {
    opacity: 1;
    filter: blur(0) contrast(100%);
  }
}
.pixel-appear {
  animation: pixelAppear 1.5s ease-out forwards;
}
```

4. Custom scene logic:
   - Apply wipe effect class to background on load
   - After 2.5s, create sprite with pixel-appear effect
   - Loop 1.png and 2.png with 500ms frame delay
   - Center the sprite in the container

5. Scene completion callback: postMessage 'sc7-complete'
  </action>
  <verify>Open sc7-intro.html in browser - verify wipe effect on bg, then pixel-appear sprite loop</verify>
  <done>sc7-intro.html plays with both effects working correctly</done>
</task>

<task type="auto">
  <name>Update scenes.json to include sc7</name>
  <files>ch1/scenes.json</files>
  <action>
Add sc7 entry to the scenes array:
```json
{
  "id": "sc7",
  "name": "Scene 7",
  "path": "sc7/scene.json"
}
```
  </action>
  <verify>scenes.json is valid JSON with sc7 included</verify>
  <done>sc7 added to chapter 1 scenes list</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] scene.json created with effect configurations
- [ ] sc7-intro.html plays correctly with both effects
- [ ] Wipe effect reveals background from center outward
- [ ] Sprite appears with pixel-appear effect after background
- [ ] Sprite loops 1.png and 2.png correctly
- [ ] scenes.json includes sc7
</verification>

<success_criteria>
- Scene 7 fully functional with wipe and pixel-appear effects
- Both scene.json and standalone HTML player working
- Registered in chapter scenes list
- Ready for chapter player integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-ch1/01-02-SUMMARY.md`
</output>
